import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'LoNacionalApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
